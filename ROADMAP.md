# Roadmap

Post-0.1 direction. v0.1 scope lives in VISION.md + PLAN.md.

## 0.2+
- Importer addon (Xaero's / JourneyMap regions + waypoints; CLI + in-game)
- Waypoint sets + sharing (export/import strings)
- Slime chunk layer (first-party or example addon)
- Biome color accuracy pass
- In-world waypoint beacons

## Toward 1.0
- Nether / cave world-map modes
- Full freeform HUD arrange screen + config search
- Performance passes vs SPEC §6 budgets on low-end hardware
- API stabilization sweep → freeze → **1.0 = Xaero feature parity + API v1 (semver, deprecation cycles)**

## Someday / addon territory
- Theme sharing/browser (addon)
- Server-interaction features beyond capability handshake (addons)
- Additional loaders — only if demand proves it
